#include "main.h"

int	true_min(SDL_point from, SDL_Point *points, size_t amount)
{
	size_t	ix;
	int		dx, dy;
	long long	min;

	ix = 0;
	min =
	while (ix < amount)
	{
		dx = from.x - point[ix].x;
		dy = from.y - point[ix].y;


		ix++;
	}
}
